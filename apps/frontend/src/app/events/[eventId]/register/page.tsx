import RegisterPage from '@/modules/cloud-enthusiasts/events/RegisterPage';

export const metadata = {
  title: "Register for Event - AWS SBG REC",
  description: "Fill in registration details and answer custom event forms to reserve your seat pass.",
};

export default function Page() {
  return <RegisterPage />;
}
